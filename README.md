# Elixion Homebrew Tap

Install Elixion CLI tools via Homebrew.

## Usage

```bash
brew tap elixion-ai/tap https://github.com/Elixion-ai/homebrew-tap
brew install elixion-bridge
```

Formulas are auto-published by GoReleaser on each bridge release.
